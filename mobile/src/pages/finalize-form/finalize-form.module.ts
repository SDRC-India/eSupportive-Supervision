import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { FinalizeFormPage } from './finalize-form';

@NgModule({
  declarations: [
    // FinalizeFormPage,
  ],
  imports: [
    IonicPageModule.forChild(FinalizeFormPage),
  ],
  exports: [
    // FinalizeFormPage
  ]
})
export class FinalizeFormPageModule {}
